# Operação v0.27

Comandos principais:

```bash
./scripts/go_live_v027.sh preflight
./scripts/go_live_v027.sh deploy
./scripts/go_live_v027.sh smoke
./scripts/go_live_v027.sh status
```

Não execute `deploy` sem backup e sem preencher os Secrets de produção.

Docker Compose permite aguardar dependências saudáveis e limitar o acesso aos Secrets por serviço; a v0.27 mantém esse modelo. citeturn0search1turn0search4
