[pytest]
pythonpath = .
testpaths = tests
markers =
    e2e: end-to-end tests requiring external services
    integration: integration tests requiring service containers
    qa: deterministic QA/invariant tests
